// The purpose of this file is to provide a global coloring scheme,
// a single place for defining colors for the whole app.

const colors = {
    mainColor: '#0275d8',
    textColorLight: 'white',
    textColorDark: 'grey',
    listBackground: '#f2f2f2',
};

export default colors;
